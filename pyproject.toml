[project]
name = "research-agent"
version = "0.1.0"
description = "Research agent"
readme = "AGENT.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "python-dotenv>=1.2.2",
    "wikipedia",
]
